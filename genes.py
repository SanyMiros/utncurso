archivo = open("gen1_p53.txt", "r")
print(archivo.read())

cadena = open("gen2_tnf.txt", "r")
print(cadena.read())

def descargar (acc):
    proceso = acc*2
    return (proceso)
def propiedades(proceso):
    n = len(proceso)
    return (n)
    props = [propiedades(x) for x in [seq1,seq2,seq3]]
    def figuras(tabla):

      def figura(n):
         m= n+2
      return (m)
